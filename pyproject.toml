[tool.poetry]
name = "textrec"
version = "0.1.0"
description = "simple text recognizer app"
authors = ["marcalph"]

[tool.poetry.dependencies]
python = "^3.9"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
